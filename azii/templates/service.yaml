apiVersion: v1
kind: Service
metadata:
  name: {{ include "azii.fullname" . }}
  labels:
    {{- include "azii.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azii.selectorLabels" . | nindent 4 }}
